{
  "name": "loan-calculator",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client:dev": "npm run --workspace=client dev"
  }
}
